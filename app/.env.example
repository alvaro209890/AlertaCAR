# AlertaCAR — App do usuário (frontend)

# URL da API (deixe vazio em dev — o Vite faz proxy de /api para o backend)
VITE_API_URL=

# Chave pública do WMS/WFS da SEMA-MT (mesma do backend) — usada para desenhar
# camadas ao vivo no mapa (Fase 5.2) direto do navegador, sem proxy.
VITE_SEMA_WFS_AUTHKEY=541085de-9a2e-454e-bdba-eb3d57a2f492
